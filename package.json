{
  "scripts": {
    "start": "browser-sync start --directory --files src --server . --no-open"
  },
  "dependencies": {
    "bootstrap": "^4.3.1",
    "browser-sync": "^2.26.7"
  },
  "devDependencies": {
    "eslint": "^5.16.0",
    "typescript": "^3.1.1"
  }
}
