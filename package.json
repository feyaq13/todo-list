{
  "scripts": {
    "start": "browser-sync start --directory --files src --server . --no-open",
    "lint": "eslint --fix --ignore-path .gitignore --ext .js src"
  },
  "dependencies": {
    "bootstrap": "^4.3.1",
    "jquery": "^3.4.1",
    "murmurhash3js": "^3.0.1"
  },
  "devDependencies": {
    "eslint": "^5.16.0",
    "browser-sync": "^2.26.7",
    "typescript": "^3.1.1"
  }
}
